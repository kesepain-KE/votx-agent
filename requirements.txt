# ═══════════════════════════════════════════════════════════════════════
# votx-agent 依赖清单（不含 skills/ 用户技能）
# 安装: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════════

# ── 核心运行时 (必装) ────────────────────────────────────────────────
flask>=3.0                  # web/ — Flask 后端 + SSE 流式传输
websockets>=12.0            # message/ — OneBot/NapCat 正向 WebSocket 连接

# ── 内置技能 (plugins/) ─────────────────────────────────────────────
yt-dlp>=2024.0              # plugins/download_anything — 视频/音频下载
tavily-python>=0.7.0        # plugins/tavily_search — 网络搜索/提取/爬取/深度研究
python-docx>=1.0.0          # plugins/word_docx — Word 文档创建和读取
markitdown>=0.0.1           # plugins/markdown_converter — 文档转 Markdown (PDF/Word/PPT/Excel 等)
pyyaml>=6.0                 # plugins/skill_creator — SKILL.md YAML frontmatter 验证

# ── 文档处理 (plugins/pdf_tools + plugins/kb_retriever) ─────────────
pdf2image>=1.17.0           # PDF 页面渲染为图片
pdfplumber>=0.11.0          # PDF 文本提取与信息查看
pypdf>=4.0.0                # PDF 编辑/合并/旋转/拆分
PyMuPDF>=1.24.0             # PDF 真实脱敏 redaction
reportlab>=4.0              # PDF 生成
Pillow>=10.0                # PDF 预览/图片处理
pytesseract>=0.3.10         # PDF OCR Python 绑定（仍需系统 tesseract）
numpy>=1.24                 # PDF 视觉对比

# ── 测试 ─────────────────────────────────────────────────────────────
pytest>=7.0.0               # tests/ — 单元测试与集成测试框架
