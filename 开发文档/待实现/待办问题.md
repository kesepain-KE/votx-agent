# 待办问题

最后更新：2026-05-04

## 高优先级

（无）

## 中优先级

### Vue 3 前端离线支持

当前通过 unpkg CDN 加载 `vue.global.prod.js`，离线无法使用。可考虑打包或内联。

### Web 路由模块化后回归测试

需要验证:
- 所有 route 注册完整
- 未选择用户时的错误返回
- 文件路径越权防护
- 归档操作正确性
- 配置保存后内存同步

### 依赖环境分层说明

区分 WSL Python / Windows Git Bash Python / Anaconda Python / 普通 Windows Python 的依赖差异。

## 低优先级

### 文档自动生成

可从代码自动生成: Web route 列表、Skill 列表、工具 schema 列表、命令列表。避免手工文档过期。

## 已完成

### ~~safe_path 沙箱~~ ✅ 已完成 (2026-05-03)

`skills/_common/__init__.py` 的 `check_sandbox()` 已关闭沙箱限制，允许访问任意路径。如需重新启用，取消注释即可。

### ~~network SSRF 防护~~ ✅ 已完成 (2026-05-03)

`skills/_common/__init__.py` 的 `validate_url()` + `MAX_RESPONSE_BYTES`。`skills/network/tool.py` 接入：URL 校验、`_SafeRedirectHandler` 重定向拦截、响应体流式读取 10MB 上限。

### ~~shell 危险命令拦截~~ ✅ 已完成 (2026-05-03)

`skills/_common/__init__.py` 的 `check_dangerous_command()` + `safe_working_dir()` + `sanitize_env()`。`skills/shell/tool.py` 接入全部安全校验。

### ~~DeepSeek thinking 模式 tool_calls 修复~~ ✅ 已完成 (2026-05-04)

`run/chat.py` `add_tool_call_message()` 新增 `reasoning_content` 参数；`run/engine.py` 从 response 提取 reasoning 并传入。确保 thinking 模式 + tool_calls 正常联动。

### ~~工具日志路径统一~~ ✅ 已完成 (2026-05-04)

所有读写路径统一使用 `tool_log.json`（`skills/_common/__init__.py`、`run/chat.py`、`web/routes/system.py`）。

### ~~Vue 3 WebUI 迁移~~ ✅ 已完成 (2026-05-04)

`web/templates/index.html` 从原生 JS DOM 操作迁移至 Vue 3 响应式框架（~2400 行），所有功能完整保留并新增：任务执行显示开关、运行状态指示 + 停止、思考默认展开、AI 回复置底、工具调用横向滚动、用户消息复制。

### ~~CLI/Web 命令差异~~ ✅ 已完成

CLI 和 Web 都支持 `/retry`。`/clear` 行为已对齐。
